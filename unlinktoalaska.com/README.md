## Unlink To Alaska Miami draft

This directory is an unlinked working copy based on the TC Audio Productions standalone site.

It is reachable at:

`/unlinktoalaska.com/`

### Notes

- This directory is not linked from the main `deleskie.com` navigation.
- HTML pages include `noindex, nofollow` where practical while the Miami version is being developed.
- Internal links were rewritten to stay under `/unlinktoalaska.com/`.
- The source copy excluded the standalone site's nested `.git` directory and `CNAME`.
- Existing TC Audio content and assets are intentionally left mostly intact as a starting point for the Miami-specific rewrite.
- External form submission is disabled until Miami contact details are provided.
